"""
Wildlife Monitor
================
Multimodal wildlife monitoring for camera trap imagery.

Packages:
  config     — central settings and the SystemConfig singleton (P4)
  data       — dataset acquisition and per-species loading (P1)
  models     — BioCLIP retrieval and the SAM 1 / SAM 3 / YOLO backends (P2)
  pipelines  — the abstract pipeline, three configurations, comparator (P2)
  utils      — the DetectionRecord contract and visualisation helpers
"""

__version__ = "1.0.0"
